let fullName: string = "dat";
let age: number = 18;
let job: string = "abc";

function display(fullName: string, age: number, job: string) {
    console.log(`name: ${fullName}`);
    console.log(`age : ${age}`);
    console.log(`job : ${job}`);
}
display(fullName, age, job);

